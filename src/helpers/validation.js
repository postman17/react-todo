export const checkTitle = title => title.length < 2
export const checkDescription = description => description.length < 3

export const titleErrorText = 'Title length must be more than 2'
export const descriptionErrorText = 'Description length must be more than 3'